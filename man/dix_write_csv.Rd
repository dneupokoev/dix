% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dix_write_csv.R
\name{dix_write_csv}
\alias{dix_write_csv}
\title{Запись data.frame в CSV-файл с преобразованием кодировки}
\usage{
dix_write_csv(param_in01 = NA, param_in02 = NA, param_in03 = NA)
}
\arguments{
\item{param_in01}{Data.frame для записи. Если не указан, функция ничего не делает.}

\item{param_in02}{Путь к выходному файлу (включая имя файла).}

\item{param_in03}{Формат сжатия. В настоящее время поддерживается только
\code{"gz"} для gzip-сжатия. Если не указан, файл записывается без сжатия.}
}
\value{
Невидимо возвращает \code{NULL}. Файл записывается на диск.
}
\description{
Функция сохраняет data.frame в CSV-файл с автоматическим преобразованием
кодировки в Windows-1251. Поддерживает сжатие gzip.
}
\details{
Функция автоматически преобразует все строковые столбцы из UTF-8 в
Windows-1251 (CP1251) для совместимости с Windows-приложениями (например, Excel).
}
\examples{
# Запись обычного CSV-файла
dix_write_csv(my_data, "output/data.csv")

# Запись с gzip-сжатием
dix_write_csv(my_data, "output/data.csv.gz", "gz")

}
